/** @deprecated Use EduSectionCard from primitives — kept for backward compatibility */
import { EduSectionCard } from '@/components/profile/eduConnect/primitives';

export default function PremiumSection(props) {
  return <EduSectionCard {...props} />;
}
